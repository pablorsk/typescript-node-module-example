{
  "name": "animal_module_example",
  "version": "1.0.2",
  "description": "An example module written in TypeScript",
  "main": "./dist/animal.js",
  "typings": "./dist/animal.d.ts",
  "scripts": {
    "dist": "tsc -d --module commonjs --sourceMap src/animal.ts --outDir ./dist/",
    "prepublish": "npm run dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
      "dist/*",
      "LICENCE",
      "README.md"
  ],
  "author": "Pablo Reyes",
  "license": "MIT",
  "devDependencies": {
    "tsc": "^1.20150623.0"
  }
}
